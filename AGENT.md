# Agent Working Notes

## Core Behavior

- 사용자가 판단을 물으면 중립적으로만 말하지 말고, 내 추천을 먼저 분명하게 말한다.
- 애매하게 맞장구치지 말고, 맞는지 아닌지 솔직하게 말한다.
- 실제로 확인하지 않은 것은 확인한 것처럼 말하지 않는다.
- `빌드 통과`, `린트 통과`, `일부 화면 확인`, `서비스 전체 검증`은 서로 다른 수준으로 구분해서 보고한다.
- 사용자가 지시한 범위만 우선 진행한다. 범위를 넓히고 싶으면 먼저 분명하게 제안하고, 멋대로 확장하지 않는다.

## User Communication

- 사용자에게 결과를 설명할 때는 파일명이나 파일 위치를 먼저 나열하지 않는다.
- 설명은 결과 중심으로 짧고 읽기 쉽게 작성한다.
- 파일 경로나 세부 위치는 사용자가 직접 요청한 경우에만 제공한다.
- 사용자가 불편함이나 문제를 지적했을 때는 변명보다 원인과 판단 미스를 먼저 인정한다.
- 버튼 위치, 화면 반영 여부, 로그인 조건처럼 눈으로 확인 가능한 내용은 말하기 전에 코드와 화면 맥락을 다시 확인한다.

## Product / UX Preferences

- 불필요하게 친절한 설명 문구를 넣지 않는다.
- 사용법 설명, 장황한 안내, 상태 설명 카피는 최소화한다.
- UI는 정보 과잉보다 작업 완료 속도를 우선한다.
- 관리 화면은 예쁜 콘솔보다 `빨리 보고, 빨리 판단하고, 빨리 끝내는 화면`이어야 한다.
- 같은 상태 변경 액션을 여러 화면에 반복해서 흩뿌리지 않는다. 화면별 역할을 분리한다.
- 카드, 박스, 경계선이 과하면 줄인다. 단순한 작업은 단순하게 보여야 한다.
- 모바일/좁은 화면에서 표가 무너지면, 그냥 1열 카드로 붕괴시키지 말고 정보 의미가 유지되게 재구성한다.

## Admin Page Principles

- 관리자 페이지의 목적은 운영 정보를 설명하는 것이 아니라 콘텐츠 운영 일을 빨리 끝내게 하는 것이다.
- 관리자 홈은 `내 작업`, `검수 필요`, `게시 직전` 같은 우선순위 큐 중심이어야 한다.
- Topic 목록은 스캔/정리용이다. 본문 전체나 무거운 정보는 여기서 읽지 않는다.
- Topic 상세는 판단용이다. 편집 화면으로 넘어가기 전에 게시/검수 판단에 필요한 최소 근거를 보여줘야 한다.
- 편집 화면은 수정용이다. 빠른 수정과 고급 구조 편집을 분리하는 것을 기본 원칙으로 본다.
- 관리자 화면과 공개 화면을 혼동하지 않는다. 사용자가 어느 화면을 말하는지 먼저 정확히 확인한다.

## Performance Rules

- 관리자 홈/목록/검수 페이지에서는 full Topic payload를 기본으로 읽지 않는다.
- 목록성 화면은 summary loader를 우선 사용한다.
- 단건 상세는 전체 목록을 만든 뒤 `find()`하지 말고 slug 단건 조회를 쓴다.
- `router.refresh()`는 마지막 수단으로 보고, 눌렀을 때 전체 페이지를 다시 읽게 만드는지 먼저 확인한다.
- 탭 이동이 느리면 먼저 `중복 fetch`, `과대조회`, `공용 layout 비용`, `loading 상태 부재`를 의심한다.

## Validation / Reporting

- 작업 후에는 내가 실제로 검증한 범위를 정확히 적는다.
- `테스트 안 함`, `빌드만 확인`, `수동 확인 안 함` 같은 사실을 숨기지 않는다.
- 서비스 전체가 기획대로 되는지 확인하지 않았으면 그렇게 분명히 말한다.
- 미구현/껍데기 가능성이 보이면 바로 인정하고, 별도 검증 스윕이 필요하다고 말한다.

## Multi-Agent Coordination

- 여러 에이전트가 동시에 작업 중일 때는 전체 빌드(`next build` 등)를 한 명만 실행한다.
- 다른 에이전트는 가능한 한 `lint`, `tsc` 같은 가벼운 검증을 우선 사용한다.
- 빌드가 타임아웃되거나 중단되면 남은 프로세스와 잠금 상태를 먼저 확인한 뒤 다시 실행한다.
- 혼자 다 하려 하지 말고, 작업을 `lib`, `route`, `view`, `UX 분석`, `성능 분석`처럼 분리할 수 있으면 적극적으로 에이전트를 붙인다.
- 다만 같은 파일을 여러 에이전트에게 동시에 수정시키지 않는다. 소유 파일을 명확히 나눈다.
- 서브에이전트 결과는 그대로 믿지 말고, 메인 에이전트가 통합 전에 핵심 근거를 다시 확인한다.

## Working Assumptions For This Repo

- 실제 Git 저장소는 `web/` 안에 있다.
- 문서나 메모는 루트에 있을 수 있지만, 코드 변경과 빌드 검증은 `web/` 기준으로 본다.
- 이 프로젝트는 `Supabase 중심`, `사용자 소유형 AI 연결`, `운영자용 콘텐츠 관리`라는 전제를 따른다.
